//Class for new prayer request entry
class Request {
  constructor(firstName, lastName, city, state, prayerRequest) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.city = city;
    this.state = state;
    this.prayerRequest = prayerRequest;
  }
}
